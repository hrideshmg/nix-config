## Purpose

This setup is not meant for mult-user setups. I only run a single user on my hosts and this folder corresponds to the home manager configuration of that single user.

In practice this means that each user is tightly coupled to the host it belongs to.

hridesh -> zenbook14 (daily driver)
operator -> workstation (homeserver)
